/* 
    @tour an interface to type a single tour
 */

export interface tour{
    id: string,
    title: string,
    dest: string,
    desc: string,
    desc_img: string,
    start_date: Date,
    end_date: Date
}
